class Hw01{
	public static void main(String[] args) {
		double[]a = new double[12];
		double tot = 0;
		double avg = 0;
		int max = a[0];
		int min = a[0];

		for (int i = 0; i < a.length; i++) {
			tot += sum[i]; // 1년 전체 사용금액		
		}
		avg = tot / a.length;
		for (int i = 1; i < a.length; i++) {
			if (a[i] > max) {
				max = a[i];
			}
		}
	}
}
